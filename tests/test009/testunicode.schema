# encoding: shift_jis
# name: unicode
{
   "Person": Struct(firstname=String(),
                    lastname=String(),
                    gender=String(default="未定", choices=["未定", "男", "女"]))
}
